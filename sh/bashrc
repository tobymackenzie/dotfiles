if test -z "${TJMDOT}"; then
	TJMDOT=$(dirname $(dirname $(readlink -f "${BASH_SOURCE[0]}")))
	export TJMDOT
fi
#--load all conf
if [ -z "$TJMENVLOADED" ]; then
	. "${TJMDOT}/sh/env.sh"
fi
. "${TJMDOT}/sh/load.sh"
