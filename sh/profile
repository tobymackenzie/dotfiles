if [ -z "$TJM_ENVLOADED" ]; then
	if [ -n "${TJM_DOTFILES_PATH}" ]; then
		. "$TJM_DOTFILES_PATH/sh/env.sh"
	else
		. "$HOME/.dotfiles/sh/env.sh"
	fi
fi
if [ -f "$TJM_DOTFILES_PATH/sh/load.sh" ] && [ -n "$PS1" ]; then
	. "$TJM_DOTFILES_PATH/sh/load.sh"
fi
