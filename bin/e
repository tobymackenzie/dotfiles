#!/bin/sh
#--load aliases
[ -n "$TJM_DOTFILES_PATH" ] && . "$TJM_DOTFILES_PATH/sh/scriptload.sh"

#--edit: edit argument(s) in `vi`. If passed a command, will run that command and pipe to `vi`.  Otherwise, pass as arguments to `vi`.
editor="${VISUAL:-${EDITOR:-vi}}"
if [ -s /dev/stdin ]; then
	usingStdin=1
else
	usingStdin=0
fi
if [ ! -e "$1" ] && ([ -x "$(command -v "$1")" ] || type "$1" >/dev/null 2>&1); then
	useCmd=1
else
	useCmd=0
fi
if [ "$usingStdin" = 1 ] || [ "$useCmd" = 1 ]; then
	dashOpt=''
	ro=''
	case "$editor" in
		vi|vim|gvim|mvim)
			addDash=1
			for opt in "$@"; do
				if [ "$opt" = '-' ]; then
					addDash=0
				fi
			done
			if [ "$addDash" = 1 ]; then
				dashOpt='-'
			fi
			ro='-R'
		;;
	esac
	if [ "$usingStdin" = 1 ]; then
		set -- "$ro" "$@" "$dashOpt"
		$editor "$@"
	else
		cmd="$1"
		shift
		args="$*"
		set -- "$ro" "$dashOpt"
		echo "$cmd \"$args\" | $editor \"$*\""
		if [ -n "$args" ]; then
			eval "$cmd $args" | $editor "$@"
		else
			eval "$cmd" | $editor "$@"
		fi
	fi
else
	$editor "$@"
fi
