#!/bin/sh
if [ ! -d "${TJMPROJ}/${1}" ]; then
	_tjmConfirm "${1} repo not installed.  Install?" && git clone "https://github.com/${TJMGHUSER:-tobymackenzie}/${1}.git" "${TJMPROJ}/${1}" || exit 1
fi
proj="$1"
shift
sub="$1"
shift
"${TJMPROJ}/$proj/$sub" "$@"
