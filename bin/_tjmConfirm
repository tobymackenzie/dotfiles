#!/bin/sh
printf '%s [yN]: ' "$*"
oldstty=$(stty -g)
stty raw echo min 1 time 0
ans=$(dd bs=1 count=1 2> /dev/null)
stty "$oldstty"
echo ''
if [ "$ans" != "${ans#[Yy]}" ]; then
	exit 0
else
	exit 1
fi
